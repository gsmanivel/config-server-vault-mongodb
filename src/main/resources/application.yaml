

server:
  port: 8081

#spring:
#  data:
#    mongodb:
#      uri: mongodb://localhost/manman

spring:
  profiles:
    active: vault
  application.name: gateway

  cloud.config.server:
    vault:
      host: localhost
      port: 8200
      scheme: http
      kv-version: 2
      authentication: TOKEN
      token: hvs.3L3rEhYSDHQRbFkx7CXu7G6e

  data:
    mongodb:
      uri: mongodb://localhost/manman